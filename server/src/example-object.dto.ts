
// acts more like an interface. It is more acceptable to use classes rather than interfaces because classes are maintained in compilation
export class ExampleObject {

    username: string;
    email: string | undefined | null;
    phoneNumber: string | undefined | null;

}